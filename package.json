{
  "name": "rin",
  "module": "rin.ts",
  "type": "module",
  "bin": {
    "rin": "./cli.ts"
  },
  "scripts": {
    "build": "bun build --compile --minify --outfile=rin cli.ts",
    "test": "bun test",
    "typecheck": "bunx tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
